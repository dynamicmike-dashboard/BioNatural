{
  "name": "bionatural-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "node ./automation/asset-halt.js && node ./automation/image-optimize.js && npm run build --workspaces",
    "prebuild": "node ./automation/asset-halt.js && node ./automation/image-optimize.js"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
